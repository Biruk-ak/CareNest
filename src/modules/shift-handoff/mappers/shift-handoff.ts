import type { ShiftHandoffCreateInput, ShiftHandoffRecord, ShiftHandoffSummary } from '../types/shift-handoff';

export interface ShiftHandoffDto {
  id: string;
  title: string;
  description: string;
  status: string;
  priority: string;
  visibility: string;
  residentId: string;
  caregiverId?: string;
  tags: string[];
  notes: string[];
  metadata: Record<string, string | number | boolean | null>;
  createdAt: string;
  updatedAt: string;
  facilityId: string;
}

export function toShiftHandoffDto(record: ShiftHandoffRecord): ShiftHandoffDto {
  return {
    id: record.id,
    title: record.title,
    description: record.description,
    status: record.status,
    priority: record.priority,
    visibility: record.visibility,
    residentId: record.residentId,
    caregiverId: record.caregiverId,
    tags: [...record.tags],
    notes: [...record.notes],
    metadata: { ...record.metadata },
    createdAt: record.createdAt,
    updatedAt: record.updatedAt,
    facilityId: record.facilityId,
  };
}

export function fromShiftHandoffForm(values: Record<string, unknown>): ShiftHandoffCreateInput {
  return {
    title: String(values.title ?? ''),
    description: String(values.description ?? ''),
    residentId: String(values.residentId ?? ''),
    caregiverId: values.caregiverId ? String(values.caregiverId) : undefined,
    tags: Array.isArray(values.tags) ? values.tags.map(String) : [],
    notes: Array.isArray(values.notes) ? values.notes.map(String) : [],
  };
}

export function mapShiftHandoffSummaryView1(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-1',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 1 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView2(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-2',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 2 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView3(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-3',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 3 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView4(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-4',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 4 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView5(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-5',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 5 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView6(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-6',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 6 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView7(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-7',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 7 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView8(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-8',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 8 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView9(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-9',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 9 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView10(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-10',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 10 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView11(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-11',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 11 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView12(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-12',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 12 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView13(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-13',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 13 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView14(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-14',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 14 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView15(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-15',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 15 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView16(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-16',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 16 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView17(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-17',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 17 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView18(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-18',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 18 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView19(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-19',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 19 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView20(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-20',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 20 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView21(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-21',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 21 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView22(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-22',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 22 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView23(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-23',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 23 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView24(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-24',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 24 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView25(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-25',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 25 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView26(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-26',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 26 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView27(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-27',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 27 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView28(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-28',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 28 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView29(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-29',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 29 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView30(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-30',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 30 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView31(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-31',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 31 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView32(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-32',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 32 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView33(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-33',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 33 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView34(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-34',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 34 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView35(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-35',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 35 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView36(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-36',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 36 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView37(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-37',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 37 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView38(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-38',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 38 - summary.overdue * 2),
  };
}

export function mapShiftHandoffSummaryView39(summary: ShiftHandoffSummary): Record<string, number | string> {
  return {
    view: 'shift-handoff-summary-39',
    total: summary.total,
    active: summary.active,
    critical: summary.critical,
    completedThisWeek: summary.completedThisWeek,
    overdue: summary.overdue,
    healthIndex: Math.max(0, 100 - summary.critical * 39 - summary.overdue * 2),
  };
}
